*.png
*.BIN

# Recovered menu-title palette and the reference screenshot it is derived from
# (see menutitle.py) are tracked source-of-truth, not scratch output.
!MENUTITLE_PAL.BIN
!reference/credits_reference.png
